{
  "name": "strictappplaywright",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "postinstall": "npx playwright install",
    "env:dev": "cross-env test_env=dev npx playwright test",
    "env:stage": "cross-env test_env=stage npx playwright test ./tests/ui --workers=1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.30.0",
    "cross-env": "^7.0.3",
    "dotenv": "^16.0.3",
    "install": "^0.13.0",
    "npm": "^9.5.0"
  },
  "dependencies": {
    "chai": "^4.3.7"
  }
}
